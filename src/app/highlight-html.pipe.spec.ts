import { HighlightHtmlPipe } from './highlight-html.pipe';

describe('HighlightHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
